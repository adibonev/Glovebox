/**
 * @glovebox/core — domain logic, repositories, types and validation.
 * The single source of truth for behaviour (ADR-0001).
 *
 * Phase 0 placeholder: no product logic yet. The first real module is Reminder
 * (`dueReminders(serviceRecords, windows, today)`), built test-first in Phase 1
 * per docs/adr/0007-reminder-core-module-tdd.md.
 */

export const CORE_PACKAGE_NAME = "@glovebox/core" as const;
